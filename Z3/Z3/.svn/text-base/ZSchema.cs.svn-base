/*
using System;
using System.Collections.Generic;
using System.Data;
using System.Data.SqlServerCe;
using System.IO;
using System.Text;
using Z3.Model;

namespace Z3.Leger
{
    public class BadZSchema
    {*/
        /*
        private void initWorkspace() {
            getBaseEntities("Container");
            getBaseEntities("Countable");
        }*/

        /*public List<ZEntity> getBaseEntities(string table) {
            List<ZEntity> retval = new List<ZEntity>();

            foreach (ZLevel l in ZLevel.Types(table)) {
                retval.AddRange(getBaseEntitiesForLevel(table, l));
            }

            return retval;
        }
        
        public List<ZEntity> getBaseEntitiesForLevel(string table, ZLevel l) {
            List<ZEntity> retval = new List<ZEntity>();

            using (SqlCeCommand cmd = _conn.CreateCommand()) {
                cmd.CommandText = "select * from " + l.Name + table;
                using (SqlCeDataReader r = cmd.ExecuteReader()) {
                    while (r.Read()) {
                        retval.Add(ZEntity.Factory(table).fromFactoryReader(l.ID, r));
                    }
                }
                return retval;
            }
        }*/
/*
    }
}
*/